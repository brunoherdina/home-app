node_modules/
.expo/
dist/
web-build/

# CNG — gerados por `npx expo prebuild`
android/
ios/

# segredos: vivem no servidor, nunca no repo
.env
.env.*
!.env.example

# worktrees efêmeras de agente — checkout, não fonte
.claude/worktrees/

*.log
.DS_Store
